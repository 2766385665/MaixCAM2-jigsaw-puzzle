# 拼图装置提交历史记录

记录范围：`master` 分支从首个基线到当前易读版提交。提交时间以 Git 记录为准。

## 逐次修改

| 提交 | 日期 | 修改内容与影响 |
| --- | --- | --- |
| `0ea0832` | 2026-07-30 | 建立项目基线。加入 MaixCAM2 第一版碎片识别、第二版视觉/求解/运动协议、PC 滑轨仿真和批量验证脚本，并保存样例图片、JSON 和运行说明。此提交定义了项目的原始目录布局。 |
| `b1801f2` | 2026-07-31 | 修复视觉流程和 V3 求解器超时问题。`piece_vision.py` 增强透视/轮廓处理，`puzzle_solver_v3.py` 增加超时控制和候选布局筛选，`main.py` 同步新的求解入口。主要目标是让复杂碎片在有限时间内返回可诊断结果。 |
| `8be674b` | 2026-07-31 | 完善 MaixCAM 运行时。运动协议增加实际搬运流程支持，基础求解器和纹理匹配器补充运行时约束；同时同步第一版识别程序的说明和兼容修改。 |
| `6fbccab` | 2026-07-31 | 强化运动计划与诊断。增加吸取点安全距离、目标区域边界/碰撞检查、纹理候选分析和单次采集工具；扩展全局求解、视觉回归、纹理匹配、运动净空验证脚本，并更新方案验证文档。 |
| `24c7d5f` | 2026-07-31 | MaixCAM2 运行时检查点（上一版可运行基线）。更新横屏相机运行参数、应用配置和 UART0 测试程序，调整视觉/运动/求解器以适配设备现场运行。后续未提交改动继续加入曝光、稀疏几何标定、像素-脉冲标定和轨道边界检查。 |
| **易读版（本次提交）** | 2026-09-13 | 不改变核心算法和协议格式。将运行时业务流程抽到 `runtime_pipeline.py`，入口 `main.py` 聚焦 Maix 设备初始化与触摸状态机；新增中文导引注释、结构解析、历史记录和交接 README。 |

## 阅读历史的方法

```powershell
git log --oneline --decorate --all
git show --stat <commit>
git diff <old-commit> <new-commit> -- puzzle_motion_v2/maix_project
```

## 版本边界

- `24c7d5f`：交接前保留的“上一次提交”，按项目约定视为可运行版。
- 本次提交：标注为“易读版”，重点是代码组织和文档，不应被误解为新的算法版本。
- 生成的 `pc_simulator/v3_validation` 图片/JSON 属于实验产物，不能替代真实硬件回归。
