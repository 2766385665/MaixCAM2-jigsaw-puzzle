# 拼图装置代码结构与修改指南

本文是交接用的“代码地图”。建议先看总览，再按功能查表，最后阅读对应文件中的函数和测试。当前默认任务是“四块碎片拼成长方形”；七巧板等新任务应通过任务适配器接入，不要把新规则直接写进相机主循环。

## 1. 一页总览

```text
设备层 main.py
  Maix Camera / Display / Touch / UART0
  只负责设备生命周期、按钮状态和界面刷新
             | Piece[] + 图像
视觉层 piece_vision.py
  A4 角点矫正 -> 分割 -> 轮廓拟合 -> Piece
             |
任务层 task_pipeline.py
  TaskAdapter + PuzzlePipeline
  当前实现：RectanglePuzzleTask
       |                         |
求解/纹理层                  运动层
puzzle_solver_v3.py          motion_protocol.py
texture_matcher.py           吸取点、净空、STM32 帧
       |                         |
       +------------+------------+
                    |
       pixel_to_pulse_calibration.py
                    |
               UART0 -> STM32
```

核心原则：设备层不认识具体任务，任务层不直接调用 Maix API，算法层不直接操作串口。

## 2. 目录与文件职责

### 2.1 设备与运行时

| 文件 | 责任 | 什么时候修改 |
| --- | --- | --- |
| `puzzle_motion_v2/maix_project/main.py` | MaixCAM 入口；相机、显示、触摸、UART 初始化；`ready/frozen` 状态机；调用视觉和任务流程 | 修改按钮区域、界面状态、相机参数、设备初始化时 |
| `puzzle_motion_v2/maix_project/runtime_pipeline.py` | 坐标转换、脉冲边界检查、结果绘制、会话保存；保留旧矩形流程兼容入口 | 修改设备无关的保存/绘制/标定转换时；新任务优先使用 `solve_task()` |
| `puzzle_motion_v2/maix_project/task_pipeline.py` | `TaskAdapter` 接口、`PuzzlePipeline` 通用编排器、`RectanglePuzzleTask` 默认实现 | 新增七巧板或其他任务时主要修改/新增这里 |
| `puzzle_motion_v2/maix_project/app.yaml` | MaixVision 打包清单和应用元数据 | 新增设备端 `.py` 文件时必须加入 `files` 列表 |

### 2.2 视觉识别

| 文件/函数 | 输入 -> 输出 | 说明 |
| --- | --- | --- |
| `piece_vision.rectify_a4()` | 原始 BGR 图 -> A4 矫正图 | 固定输出尺寸和 A4 坐标系 |
| `piece_vision.detect_pieces()` | 矫正图 -> `list[Piece]`、二值图、阈值 | 完成亮度/颜色分割、候选筛选、编号和排序 |
| `piece_vision.fit_polygon()` | OpenCV 轮廓 -> 3~5 边多边形 | 清理毛刺、短边和近共线点 |
| `piece_vision.SparseGeometryCalibration` | 矫正坐标 <-> 相机坐标 | 只对点/轮廓做稀疏畸变修正，不重采样整张图 |
| `piece_vision.draw_detection()` | 图像 + `Piece[]` -> 标注图 | 调试识别结果，不参与求解 |
| `piece_vision.save_debug()` | 会话数据 -> 文件前缀 | 保存图像和碎片 JSON |

`Piece.polygon` 是像素坐标；默认任务在 `RectanglePuzzleTask.prepare_source()` 中除以 `PX_PER_CM` 转为厘米。新增任务可以使用自己的数据模型，但转换必须集中在适配器内。

### 2.3 求解、纹理和运动

| 文件 | 责任 | 关键入口 |
| --- | --- | --- |
| `puzzle_solver_v3.py` | 接缝族生成、拓扑枚举、刚体姿态优化、候选布局排序、超时诊断 | `solve_geometry()`、`last_diagnostics()` |
| `texture_matcher.py` | 建立图像采样上下文，对几何合法候选做纹理连续性排序 | `build_context()`、`score_source_topology()`、`score_layout()` |
| `motion_protocol.py` | 吸取点、工作区/净空检查、目标补偿、命令编码和串口发送 | `safe_pickup_point()`、`build_motion_plan()`、`send_stm32_frames()` |
| `pixel_to_pulse_calibration.py` | 实时预览像素到滑轨脉冲的固定标定映射及限位 | `screen_pixel_to_pulse_float()`、`in_calibrated_screen_area()` |
| `puzzle_solver.py` | 旧版边 DFS 求解器，作为兼容回退和历史验证使用 | 不要作为新任务默认入口 |

### 2.4 PC 验证与仿真

| 目录 | 内容 |
| --- | --- |
| `puzzle_motion_v2/pc_tests/` | 视觉回归、纹理匹配、V3 合成求解、运动净空、脉冲标定等验证脚本 |
| `puzzle_motion_v2/pc_simulator/` | 读取 `*_motion.json`，模拟 XY/Z/旋转轨迹并生成视频 |
| `puzzle_validation_output/`、`pc_simulator/v3_validation/` | 测试生成物，不是设备端输入 |

## 3. 一次完整运行的数据流

```text
START 触摸抬起
  1. main.py 读取高清帧
  2. piece_vision.rectify_a4()
  3. piece_vision.detect_pieces()
  4. piece_vision.draw_detection()
  5. ACTIVE_TASK -> PuzzlePipeline.solve()
       prepare_source -> build_texture_context -> solve
       -> diagnostics -> canonicalize -> build_motion_plan
       -> validate_plan
  6. 有计划时 runtime_pipeline.send_motion_plan() -> UART0
  7. runtime_pipeline.draw_motion_plan() 显示目标轮廓和箭头
  8. runtime_pipeline.save_session() 保存可复现会话
```

### 3.1 坐标系

| 坐标 | 单位 | 使用位置 |
| --- | --- | --- |
| 相机坐标 | 像素，当前横屏相机分辨率 | 原始帧和实时预览 |
| A4 矫正坐标 | 像素 | `piece_vision` 的轮廓处理 |
| A4 几何坐标 | 厘米 | 求解器、目标布局和运动计划 |
| 预览坐标 | 640x360 像素 | 触摸屏/显示层 |
| 滑轨坐标 | 脉冲整数 | 标定模块和 STM32 |

改动坐标系时必须同时检查 `PX_PER_CM`、稀疏几何标定、预览缩放和脉冲限位。不要在单个函数中混用像素和厘米。

### 3.2 输出数据契约

`build_motion_plan()` 返回的计划字典至少应包含：

```text
plan["pieces"]       每块碎片的 source/target 顶点、吸取点、旋转角、安全标志
plan["commands"]     发送给 motion_protocol 的结构化命令
plan["stm32_frames"] 十六进制调试帧
plan["solution"]     目标尺寸、矩形度、净空和重叠诊断
plan["solver_diagnostics"] 求解阶段统计
```

UI 读取 `pieces` 绘图，发送层读取 `commands`，保存层读取 `stm32_frames`。新任务应尽量保持这些字段；如果字段不同，应同时修改绘制、发送和保存适配器。

## 4. 如何更换为七巧板

### 4.1 不需要改的部分

- `main.py` 的相机、触摸、显示和退出循环
- `runtime_pipeline.py` 的坐标转换、会话保存和基础绘制
- UART 初始化和 `app.yaml` 的设备打包机制

### 4.2 新增任务适配器

建议新增 `puzzle_motion_v2/maix_project/tangram_task.py`，实现 `TaskAdapter` 的 8 个方法：

```python
class TangramTask:
    name = "tangram"
    def prepare_source(self, pieces): ...
    def build_texture_context(self, rectified, geometry_calibration): ...
    def solve(self, source_data, max_seconds, texture_context): ...
    def diagnostics(self): ...
    def canonicalize(self, solution): ...
    def build_motion_plan(self, source_data, solution, geometry_calibration): ...
    def validate_plan(self, plan, diagnostics): ...
    def success_message(self, plan, diagnostics): ...
```

然后在 `main.py` 中将：

```python
ACTIVE_TASK = RectanglePuzzleTask()
```

替换为：

```python
from tangram_task import TangramTask
ACTIVE_TASK = TangramTask()
```

最后将 `tangram_task.py` 加入 `app.yaml -> files`。推荐先在 PC 上调用 `PuzzlePipeline(TangramTask()).solve(...)`，通过后再烧录 MaixCAM。

七巧板通常不适合直接复用当前 V3 的“矩形外框 + 接缝边长”假设。应在 `TangramTask.solve()` 内使用独立求解器，例如目标轮廓覆盖、角点匹配或预定义模板；不要为了兼容七巧板去修改 V3 中所有矩形常量。

## 5. 按需求修改哪个文件

| 需求 | 首选修改位置 | 必须同步验证 |
| --- | --- | --- |
| 换任务类型/碎片数量/目标形状 | 新建 `xxx_task.py`，替换 `ACTIVE_TASK` | 任务适配器单测、完整运动计划 |
| 改识别阈值、碎片面积或边数 | `piece_vision.py` 常量和 `detect_pieces()` | 视觉回归、真实采集样例 |
| 改 A4 角点或镜头畸变 | `piece_vision.py`、`camera_calibration.py` | 标定样例、坐标映射检查 |
| 改接缝匹配或多解排序 | `puzzle_solver_v3.py`、`texture_matcher.py` | V3 synthetic、纹理匹配验证 |
| 改吸取点、磁铁半径或净空 | `motion_protocol.py` | motion clearance、无负载实机测试 |
| 改像素到电机脉冲 | `pixel_to_pulse_calibration.py` | pulse calibration、滑轨限位测试 |
| 改 STM32 帧字段/串口 | `motion_protocol.py`、`uart0_test.py` | PC 仿真、UART 回环和急停测试 |
| 改按钮、提示或显示布局 | `main.py`、`piece_vision.compose_screen()` | MaixCAM 触摸实机测试 |
| 新增设备端文件 | `app.yaml` | 检查打包产物包含新文件 |

## 6. 测试与交接顺序

```powershell
# 1. 语法和导入
D:\label\python\python.exe -m py_compile puzzle_motion_v2\maix_project\*.py

# 2. 几何和运动
D:\label\python\python.exe puzzle_motion_v2\pc_tests\validate_global_solver_v3_synthetic.py
D:\label\python\python.exe puzzle_motion_v2\pc_tests\validate_motion_clearance.py
D:\label\python\python.exe puzzle_motion_v2\pc_tests\validate_pixel_to_pulse_calibration.py

# 3. 有真实采集样例时再做视觉/纹理回归
D:\label\python\python.exe puzzle_motion_v2\pc_tests\validate_piece_vision_regression.py
D:\label\python\python.exe puzzle_motion_v2\pc_tests\validate_texture_matching.py
```

硬件测试顺序：无负载 UART 回环 -> 滑轨限位 -> 单块碎片低速搬运 -> 多块完整流程。任何坐标、标定或协议修改都要在提交说明中写明样例、命令和硬件结果。

## 7. 兼容入口与版本边界

`runtime_pipeline.solve_detected_pieces()` 是旧版矩形流程的兼容函数，保留它是为了不破坏历史脚本。新代码应使用 `runtime_pipeline.solve_task(..., task=...)` 或 `PuzzlePipeline(task)`，这样替换任务时不会误调用旧矩形规则。

`24c7d5f` 是上一版可运行基线；`210fab7` 是易读版；`8197ac1` 是任务适配器模块化提交。实验图片、视频、压缩包和构建输出不属于运行时源代码。
