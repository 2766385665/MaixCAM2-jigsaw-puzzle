# 拼图装置交接说明（按文件查找版）

这份说明给第一次接手项目的人看。先记住一句话：**真正运行的入口只有 `puzzle_motion_v2/maix_project/main.py`；其他文件分别负责识别、计算、搬运、测试或记录。**

## 先看这张图

```text
相机画面
   ↓
main.py                 读取相机、显示画面、等待触摸
   ↓
piece_vision.py         找到每一块碎片，输出 Piece
   ↓
task_pipeline.py       选择当前任务（现在是矩形拼图）
   ├─ puzzle_solver_v3.py   计算碎片应该怎么拼
   ├─ texture_matcher.py    图案相似时帮助挑选正确答案
   └─ motion_protocol.py    计算吸哪里、怎么移动、发什么指令
          ↓
pixel_to_pulse_calibration.py   把画面位置换成电机脉冲
          ↓
UART0 → STM32 → 滑轨和电磁铁
```

如果只是“换任务”，改 `task_pipeline.py` 和新增任务文件；如果只是“识别不准”，先看 `piece_vision.py`；如果只是“机械动作不对”，先看 `motion_protocol.py` 和标定文件。

## 一、设备端目录：会被 MaixCAM 运行

目录：`puzzle_motion_v2/maix_project/`

| 文件 | 用大白话说明 | 需要修改时 |
| --- | --- | --- |
| `main.py` | 程序总入口。打开相机、显示屏、触摸屏和 UART；实时显示画面；点击 START 后依次调用识别、求解、发送和保存。 | 修改按钮位置、屏幕提示、相机分辨率、串口设备，或切换 `ACTIVE_TASK`。不要把求解算法直接塞进这里。 |
| `task_pipeline.py` | “任务插槽”。`PuzzlePipeline` 负责固定流程，`TaskAdapter` 规定每种任务要提供哪些函数，`RectanglePuzzleTask` 是当前默认任务。 | 做七巧板、六边形拼图等新任务时，新增 `tangram_task.py` 并替换 `main.py` 的 `ACTIVE_TASK`。 |
| `runtime_pipeline.py` | 公共工具：厘米坐标转屏幕/脉冲、画出目标轮廓、保存识别结果和运动 JSON。也保留了旧矩形流程的兼容函数。 | 修改坐标转换、结果绘图、保存格式时。新任务调用 `solve_task()`，不要调用旧的 `solve_detected_pieces()`。 |
| `piece_vision.py` | 视觉识别主文件。把相机图像拉正成 A4，分割出碎片，拟合轮廓，给碎片编号。 | 改阈值、碎片面积范围、边数、角点矫正、识别标注时。 |
| `puzzle_solver_v3.py` | 当前矩形拼图的主要计算器：比较边长、尝试拼接关系、优化位置、检查是否组成合法矩形。 | 只在仍然是“矩形拼图”时修改。七巧板不要硬改这里，应写新的任务求解器。 |
| `puzzle_solver.py` | 老版本求解器。速度和规则较简单，V3 失败时有些场景会用它兜底。 | 一般不要改；除非专门维护旧回退逻辑。 |
| `texture_matcher.py` | 看碎片上的颜色/图案是否能接上，用来解决“几何上有多个答案”的情况。 | 图案误判、白色扑克牌、多解排序问题。没有图像时它可以不启用。 |
| `motion_protocol.py` | 把拼好的目标变成真实动作：选择吸取点、检查磁铁是否会碰边、检查碎片之间是否重叠、生成 STM32 指令并发送。 | 改吸盘半径、边缘余量、搬运顺序、净空检查或串口协议时。 |
| `pixel_to_pulse_calibration.py` | 保存“屏幕上的这个点 = 滑轨的多少脉冲”的公式和限位。 | 换相机、换安装位置、换滑轨或重新标定时。不能凭感觉改，必须重新测量。 |
| `camera_calibration.py` | 镜头标定参数和稀疏矫正辅助代码。 | 重新做镜头/相机标定时。 |
| `uart0_test.py` | 单独测试 UART0 是否能向 STM32 发数据。 | 修改串口或 STM32 协议后先改/运行它。 |
| `jiuzheng.py` | 单独的曝光和相机画面测试工具，中文文件名意为“校正/调试”。 | 调曝光、观察反光和相机画面时；不属于主程序。 |
| `make_demo_plan.py` | 生成一份固定的演示运动计划 JSON，给 PC 仿真器使用。 | 想增加或修改演示动作时。 |
| `app.yaml` | MaixVision 的打包清单。里面列出的 `.py` 才会被装进设备。 | 新增任何设备端 Python 文件后必须加入 `files`。 |
| `app.png` | MaixVision 应用图标。 | 只想换图标时修改。 |

### `sg/` 子目录

`sg/main.py` 和 `sg/pixel_to_pulse_calibration.py` 是另一套独立的像素到滑轨标定实验程序。它们不是当前拼图主入口，除非明确要重新标定，否则不要和主目录同名文件混用。

## 二、第一版和历史文件：可以参考，不要当主程序

目录：`maixcam2_first_version/`

| 文件 | 说明 |
| --- | --- |
| `maixcam2_piece_detector.py` | 最早的 MaixCAM 碎片识别程序，适合了解最初的相机和触摸写法。 |
| `maixcam2_piece_detector_v1_2_slow_backup.py` | 上一个较慢版本的备份。只用于对比，不能直接覆盖当前版本。 |
| `MAIXCAM2_碎片识别第一版说明.md` | 第一版识别方法和现场使用记录。 |

根目录的 `README_运行说明.md`、`拼图装置方案验证.md`、`提交历史记录.md` 和 `puzzle_motion_v2/*.md` 是历史说明/实验记录；内容可能对应旧版本，遇到冲突以 `main.py`、`task_pipeline.py` 和当前测试结果为准。

## 三、PC 测试和仿真文件：不会控制真实电机

目录：`puzzle_motion_v2/pc_tests/`

| 文件 | 测什么 |
| --- | --- |
| `validate_global_solver_v3_synthetic.py` | 用随机生成的碎片测试 V3 能不能拼回矩形。改求解器后优先运行。 |
| `validate_global_solver_v3.py` | 用已有 JSON/样例检查 V3 求解结果。 |
| `validate_t_junction_solver.py` | 测试一条长边对接两条短边的 T 形接缝。 |
| `validate_piece_vision_regression.py` | 用历史图片检查碎片识别是否变差。 |
| `validate_640_capture_regression.py` | 检查 640x360 采样相关的识别变化。 |
| `validate_texture_matching.py` | 两块形状相同但图案不同的时候，检查纹理排序是否正确。 |
| `inspect_texture_candidates.py` | 把所有纹理候选导出成图片，方便人工比较。 |
| `validate_motion_clearance.py` | 检查目标重叠、工作区和搬运净空。 |
| `validate_pixel_to_pulse_calibration.py` | 检查屏幕像素到滑轨脉冲的标定误差。 |
| `run_single_capture.py` | 对一张保存的真实采集图重新跑当前识别/求解流程。 |

目录：`puzzle_motion_v2/pc_simulator/`

| 文件 | 说明 |
| --- | --- |
| `pc_motion_simulator.py` | 读取 `*_motion.json`，在电脑窗口/视频中模拟 XY、Z 和旋转动作，不会连接真实串口。 |
| `启动滑轨搬运仿真.cmd` | Windows 双击启动仿真器。 |
| `demo_motion_plan.json` | 演示用计划输入。 |

根目录的 `puzzle_solver_validation.py` 是更早的端到端几何验证；`puzzle_visual_demo.py` 是带动画的演示程序；`启动拼图可视化演示.cmd` 用于双击启动演示。它们适合验证算法，不是 MaixCAM 入口。

## 四、根目录的配置、文档和备份

| 文件 | 说明 |
| --- | --- |
| `.gitignore` | 告诉 Git 忽略 Python 缓存、测试输出、视频和构建目录。新增生成目录时在这里补规则。 |
| `requirements-puzzle.txt` | PC 测试依赖：NumPy、OpenCV、PyQt5、Shapely。MaixCAM 的 `maix` 使用固件自带环境。 |
| `.vscode/launch.json` | VS Code 调试启动配置。只影响本地调试，不影响 MaixCAM。 |
| `.vscode/settings.json` | VS Code 编辑器设置。 |
| `README.md` | 面向所有人的快速开始、运行方式和不足说明。 |
| `README_运行说明.md` | 较早的运行说明，文字可能对应旧版本。 |
| `代码结构解析.md` | 本文件，面向接手者的逐文件地图。 |
| `提交历史记录.md` | 每次 Git 提交改了什么，以及“可运行版/易读版”的版本边界。 |
| `拼图装置方案验证.md` | 方案验证和实验结论。 |
| `puzzle_motion_v2/README_拼图求解与滑轨仿真.md` | v2 目录的设备操作、坐标和仿真说明。 |
| `puzzle_motion_v2/V3全局求解器说明.md` | V3 求解器的算法实验记录和参数解释。 |
| `puzzle_motion_v2/T形接缝求解说明.md` | T 形接缝（长边接两短边）的专项说明。 |
| `puzzle_motion_v2/validation_summary.json`、`v3_validation_summary.json` | 历史验证结果摘要，程序不会读取它们。 |
| `软件/视觉设计报告` 类 `.docx` | 设计报告文档，不参与程序运行；修改算法后可补充实验结论。 |
| `MaixCAM2_Puzzle_first_commit_0ea0832.zip` | 首次提交的压缩备份。需要对照历史时解压查看，不要覆盖当前目录。 |
| `_cleanup_scan.ps1`、`_cleanup_scan2.ps1` | 临时清理/扫描脚本。运行前先阅读内容，避免误删实验资料。 |

## 五、最重要的“怎么改”

### 1. 换成七巧板

1. 新建 `puzzle_motion_v2/maix_project/tangram_task.py`。
2. 写一个 `TangramTask`，实现 `TaskAdapter` 中的 8 个函数：准备碎片、建立图像特征、求解、返回诊断、整理结果、生成运动计划、检查安全、生成提示文字。
3. 在 `main.py` 中把：

   ```python
   ACTIVE_TASK = RectanglePuzzleTask()
   ```

   改成：

   ```python
   from tangram_task import TangramTask
   ACTIVE_TASK = TangramTask()
   ```

4. 在 `app.yaml` 的 `files` 中加入 `tangram_task.py`。
5. 先用 PC 测试，再上 MaixCAM。七巧板的目标通常不是矩形，建议使用新的目标轮廓/模板求解器，不要修改 V3 的矩形尺寸常量。

### 2. 只改视觉识别

先改 `piece_vision.py`，再运行 `validate_piece_vision_regression.py`。不要同时改求解器，否则无法判断问题来自识别还是拼接。

### 3. 只改机械动作

先改 `motion_protocol.py` 或 `pixel_to_pulse_calibration.py`，运行净空和脉冲标定测试，再做无负载 UART 测试。真实设备测试必须按“限位 -> 单块 -> 多块”逐步增加风险。

### 4. 只改界面

改 `main.py` 的按钮/状态，或改 `piece_vision.compose_screen()` 的画面。界面改动不应改变 `Piece`、`plan["pieces"]` 和 `plan["commands"]` 的字段。

## 六、交接时的运行顺序

```text
1. PC 语法检查
2. V3 合成求解测试
3. 运动净空和脉冲标定测试
4. 有真实图片时做视觉/纹理回归
5. MaixCAM 无负载 UART
6. 滑轨限位
7. 单块碎片低速搬运
8. 多块完整任务
```

设备端保存目录默认为 `/root/puzzle_motion_output`。一次运行通常会得到原图、A4 矫正图、二值图、标注图、`*_motion.json`、`*_stm32.txt` 和 `*_solver_diagnostics.json`；排查问题时应把这一整组文件一起交给接手人。

## 七、不要混淆的文件

- `main.py` 是设备运行入口；`puzzle_visual_demo.py` 是 PC 动画入口。
- `puzzle_solver_v3.py` 是矩形拼图算法；`task_pipeline.py` 才是更换任务的入口。
- `pixel_to_pulse_calibration.py` 是当前主程序使用的标定；`sg/` 下的是独立实验版本。
- `pc_simulator` 只模拟，不代表真实电机已经安全。
- `puzzle_validation_output/`、`v3_validation/`、`dist/`、图片、视频和压缩包是输出/备份，不是必须修改的源代码。
