# 拼图装置代码结构解析

## 总体框架

```text
相机帧
  -> main.py（Maix 设备与触摸状态机）
  -> piece_vision.py（A4 矫正、分割、轮廓和碎片编号）
  -> runtime_pipeline.py（坐标转换、求解编排、绘制、保存）
       -> texture_matcher.py（接缝纹理评分）
       -> puzzle_solver_v3.py（全局几何布局搜索）
       -> motion_protocol.py（吸取点、净空、STM32 帧）
       -> pixel_to_pulse_calibration.py（屏幕像素到滑轨脉冲）
  -> UART0 / STM32 滑轨执行
```

## 功能与文件对应关系

| 功能 | 主要文件 | 修改入口 |
| --- | --- | --- |
| MaixCAM 启动、相机、显示、触摸 | `puzzle_motion_v2/maix_project/main.py` | `run_maix()`；只处理设备生命周期和状态切换 |
| 运行时业务编排 | `puzzle_motion_v2/maix_project/runtime_pipeline.py` | `solve_detected_pieces()`、`save_session()` |
| A4 透视矫正和碎片识别 | `puzzle_motion_v2/maix_project/piece_vision.py` | `rectify_a4()`、`detect_pieces()`；阈值/轮廓问题优先改这里 |
| 接缝纹理匹配 | `puzzle_motion_v2/maix_project/texture_matcher.py` | `build_context()`、`score_layout()`；图案相似导致歧义时改这里 |
| 几何全局求解 | `puzzle_motion_v2/maix_project/puzzle_solver_v3.py` | `solve_geometry()`；超时和候选布局问题改这里 |
| 备用几何求解器 | `puzzle_motion_v2/maix_project/puzzle_solver.py` | 仅用于旧验证/兼容，不是当前 Maix 默认入口 |
| 吸取点与运动安全 | `puzzle_motion_v2/maix_project/motion_protocol.py` | `safe_pickup_point()`、`build_motion_plan()` |
| 相机坐标到滑轨脉冲 | `puzzle_motion_v2/maix_project/pixel_to_pulse_calibration.py` | 标定参数和 `screen_pixel_to_pulse_float()` |
| 镜头/稀疏几何标定 | `puzzle_motion_v2/maix_project/piece_vision.py`、`camera_calibration.py` | 重新采集标定点后更新参数 |
| PC 回归与仿真 | `puzzle_motion_v2/pc_tests/`、`puzzle_motion_v2/pc_simulator/` | 先运行验证脚本，再改设备端逻辑 |

## 一次 START 点击的流程

1. 读取高清帧，保存原始图像。
2. `rectify_a4()` 将工作面变换到统一 A4 坐标。
3. `detect_pieces()` 完成分割、轮廓拟合和编号。
4. `runtime_pipeline.solve_detected_pieces()` 调用 V3 求解器；有图像时同时建立纹理上下文。
5. `motion_protocol.build_motion_plan()` 计算目标位姿、吸取点、净空和 STM32 命令；标定越界会中止发送。
6. 立即显示结果，再由 `save_session()` 保存图片、`*_motion.json`、`*_stm32.txt` 和 `*_solver_diagnostics.json`。

## 修改建议

- 改算法前先在 `pc_tests` 增加可重复样例，避免直接在 MaixCAM 上试错。
- 改坐标系时同步检查 `piece_vision.PX_PER_CM`、稀疏标定和像素-脉冲标定，三者必须使用同一相机分辨率。
- 改 UART 协议时同时更新 `motion_protocol.py`、`uart0_test.py` 和 PC 仿真器，确认帧校验及急停行为。
- 不要把实验输出目录中的图片/JSON 当作算法输入；正式输入应通过采集脚本或 MaixCAM 相机产生。
