# E题拼图装置方案与算法验证

## 1. 题目关键约束

- 在纵向 A4 纸上工作；碎片初始随机、不重叠地放在上半区，目标矩形位于下半区。
- 一键启动、全自动运行，中途不得人工干预；最长时间 2 分钟。
- 前两题现场碎片不超过 4 片，每片不超过 5 条边，每条边不短于 1 cm。
- 每片至少有一条边或边的一部分位于目标矩形外边；同一碎片可以同时包含多条外边界段。
- 现场碎片为镀锌薄铁片覆纸，总厚度不超过 2 mm、质量不超过 30 g。
- 目标矩形尺寸范围：长边 9～12 cm，短边 5～9 cm。
- 现场包含纯白几何拼图和白底扑克牌图案拼图两类。
- 相邻碎片不能重叠，对应顶点距离不大于 2 cm；扑克牌题还要求花纹对应。

## 2. 方案结论

“MaixCAM 2 + 滑轨”方案合理，但必须满足以下条件：

1. 摄像头建议固定在 A4 正上方，不要跟随滑轨移动。
2. 执行机构不能只有 XY 两轴，必须具备 XY 平移、Z 抬升和 θ 旋转自由度。
3. 利用现场碎片为镀锌铁片的条件，优先采用小型电磁铁吸取头。
4. 上位视觉一次完成全局识别和规划，机械端按绝对目标位姿依次搬运。

若当前滑轨只有单轴或 XY 两轴且吸头不能旋转，则方案不能稳定完成随机姿态拼图。

## 3. 推荐硬件结构

### 3.1 视觉

- MaixCAM 2 固定俯视整个 A4 区域。
- 1280 像素覆盖 A4 的 21 cm 宽度时，理论比例约为 61 px/cm，即 0.164 mm/px。
- 使用 A4 四角和中间分界线进行透视矫正，得到“像素坐标 → A4 毫米坐标”的单应矩阵。
- 自备 A4 纸建议使用深色、哑光、无纹理纸，便于分割白色现场碎片。
- 机械臂进入取放动作前完成拍照，或者拍照时让吸头回到画面外，避免遮挡。

### 3.2 运动机构

- X 行程建议不小于 230 mm。
- Y 行程建议不小于 320 mm。
- Z 行程只需满足可靠抬离其他碎片，建议 20～40 mm。
- θ 轴至少覆盖 ±180°，目标重复精度优于 2°。
- XY 目标重复定位精度建议优于 2～3 mm，明显严于题目 20 mm 顶点容差。
- 设置 X/Y/Z 原点限位，一键启动后自动回零。

### 3.3 吸取头

- 推荐小型电磁铁，铁片质量上限只有 30 g，吸力裕量充足。
- 吸取点选择碎片内部最大内接圆圆心，而不是简单使用轮廓重心；这样能避免吸头靠近边缘导致掉片。
- 放置时先下降到纸面上方约 1 mm，再断电释放，最后抬升。
- 需要测试剩磁导致的粘连；必要时增加反向脉冲、弹簧顶针或轻微抖动释放。

## 4. 软件流程

1. 检测 A4 边界与分界线，计算俯视矫正矩阵。
2. 截取上半区域，按 HSV/灰度阈值分割碎片。
3. 形态学去噪，提取外轮廓。
4. 使用 `approxPolyDP` 将轮廓拟合为 3～5 边多边形。
5. 记录每片的顶点、边长、质心、主方向和安全吸取点。
6. 枚举长度相符的碎片边，将一条边反向贴合到另一条边。
7. 通过回溯搜索组合不重叠的刚体变换。
8. 用以下条件筛选完整方案：
   - 拼接面积接近最小外接矩形面积；
   - 矩形长短边处于题目范围；
   - 碎片无明显重叠；
   - 所有碎片均在矩形内。
9. 扑克牌图案题在几何候选上增加接缝纹理连续性评分。
10. 将解算位姿转换到 A4 毫米坐标，生成每片的取料 `(x, y, θ)` 和放料 `(x, y, θ)`。
11. 依次执行抬升、移动、旋转、下降、释放。
12. 完成后复拍一次做位置校验，再给出声光提示。

## 5. 扑克牌纹理评分

不能仅使用边长判断扑克牌碎片，因为几何形状可能存在多个合法排列。建议对每个几何候选计算：

`总评分 = 几何缝隙代价 + 纹理灰度差 + 纹理梯度方向差 + 外矩形惩罚`

具体做法：

- 将候选碎片按解算位姿仿射变换到统一画布。
- 在每条候选接缝两侧分别采样 3～8 像素宽的窄条带。
- 比较接缝两侧的 RGB/灰度连续性和 Sobel 梯度连续性。
- 对数字、花色等高纹理位置增加权重。
- 若多个结果分数接近，可加入标准扑克牌模板匹配作为最终判据。

## 6. 软件仿真结果

验证脚本：`puzzle_solver_validation.py`

测试条件：

- 随机生成尺寸合法的目标矩形；
- 同时生成两类拓扑：一类由矩形角点向内部交汇点切分；另一类从矩形四条边的中间位置向内部切分，使每条矩形边都由两块碎片的部分边段组成；
- 第二类拓扑中每块都包含两条部分外边，因此测试不依赖“一块碎片对应一条完整矩形边”的假设；
- 覆盖三角形、四边形和五边形，且生成阶段检查每条边不短于 1 cm；
- 随机旋转并无重叠散放在 A4 上半区域；
- 以约 60 px/cm 渲染模拟相机图像；
- 添加成像噪声和轻微模糊；
- 使用 OpenCV 完成分割与轮廓拟合；
- 使用边匹配和非重叠回溯搜索恢复矩形。

固定随机种子 `20260729` 的回归结果：

| 测试项 | 结果 |
|---|---:|
| 合法拼图测试 | 1000 组 |
| 成功恢复 | 996 组 |
| 成功率 | 99.6% |
| 检出的三角形碎片 | 450 片 |
| 检出的四边形碎片 | 1869 片 |
| 检出的五边形碎片 | 1681 片 |
| 含一条矩形外边界段 | 1600 片 |
| 含两条矩形外边界段 | 2400 片 |
| 不相关碎片负样本 | 1000 组 |
| 错误接受 | 0 组 |
| 平均几何求解时间 | 28.18 ms |
| 最慢几何求解时间 | 385.07 ms |
| 合成纹理边配对准确率 | 100% |

算法计算时间远小于 2 分钟限制，系统总时间主要由四次机械取放决定。

## 7. 当前验证边界

本次结果证明几何搜索思路可行，但不等于整机已经达到 100%：

- 当前视觉数据为合成图像，尚未覆盖真实反光、阴影、镜头畸变和运动模糊。
- 仿真已覆盖 3～5 边混合碎片、完整外边和部分外边两类情况，但内部切割仍主要汇聚到一个交汇区域；还需增加 T 形、分级切割等拓扑和真实铁片测试。
- 1000 组合成图中有 4 组几何搜索失败，说明当前实现需要保留“无解时重新拍照/调整轮廓拟合参数”的降级流程，不能把仿真成功率写成 100%。
- 合成纹理测试只验证了接缝特征匹配逻辑，扑克牌真实图案必须另建实拍测试集。
- 尚未将电机回差、吸头偏心、释放滑移纳入闭环仿真。

## 8. 下一步实物验证

1. 固定 MaixCAM 2，采集至少 100 组真实随机摆放图像。
2. 测量像素到纸面坐标的重复误差，目标小于 2 mm。
3. 为每种碎片重复取放 30 次，统计掉片率和放置角度误差。
4. 先完成纯白几何题，再采集真实扑克牌碎片训练/调试纹理评分。
5. 对每片执行“放置后复拍校正”，必要时进行一次二次微调。
6. 完整流程连续运行 30 轮，目标单轮小于 60 秒、成功率不低于 95%。
